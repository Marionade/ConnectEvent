{% extends 'base.html.twig' %}

{% block title %}Event index{% endblock %}

{% block body %}
    <h1>PROCHAINS EVENEMENTS</h1>

    <table class="index">
   {#     <thead>
            <tr>
                <th>Durée</th>
                <th>Départ</th>
                <th>Lieu</th>
                <th>actions</th>
            </tr>
        </thead>#}
        <tbody>
        {% for event in events %}
            <tr>
                <h2> - {{ event.title }}</h2>
                <p>{{ event.duration }}  </p>
                <p>Départ {{ event.startDate ? event.startDate|date('d/m/y') : '' }} à {{ event.location }}</p>
                <p>
                    <a href="{{ path('app_event_show', {'id': event.id}) }}"> VOIR</a>
                    <a href="{{ path('app_event_edit', {'id': event.id}) }}">/ modifier</a>
                </p>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <button href="{{ path('app_event_new') }}">Create new</button>
{% endblock %}
